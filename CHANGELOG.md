# 更新记录

## 1.2.26 — 2026-09-20

修复助手停止后宠物仍可见、但点击不再说话的情况，并缩短点按反馈等待。

- 通过 WMI 在后台独立启动输入和说话助手，避免继承启动端的 Windows 进程作业。
- 收到点击事件后立即触发说话，取消双击等待及随后 3 秒的点击忽略逻辑。
- 保留原生鼠标输入和 Codex 点击行为。

验证：在隔离测试中关闭启动进程的作业后，助手测试进程仍继续运行；48 项点击处理、事件去重和语法检查通过。本机两个助手独立运行，副屏气泡可见，Codex 进程及所有宠物图集和清单保持不变。函数测试不代表画面显示延迟，拖拽手感仍需人工确认。

在仓库目录运行点击回归检查（只提取函数测试，不操作实际鼠标或窗口）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Test-ImmediatePetSpeech.ps1
```

## 1.2.25 — 2026-09-20

Windows 宠物在拖动过程中会改变窗口位置；辅助程序提前换算坐标再异步转发时，坐标可能在消费时发生偏移。本版改用完整原生鼠标输入，辅助程序只观察手势并触发说话。

- 删除辅助程序对鼠标按下、移动和松开消息的拦截与补发。
- 拖动期间暂停 UI Automation 扫描、气泡布局测量与窗口样式维护；样式更新统一在输入线程处理。
- 保留分层窗口渲染属性，移除竖屏副屏窗口高度限制，并避让截图选区等覆盖窗口。
- 快速连续拖动时使用实际窗口命中补充忙碌状态；识别目标暂时丢失后在空闲时恢复鼠标穿透。
- 气泡跟随宠物可见性，保留长文本自动测量；只有事件文件变化时才重新解析。
- 启动说话助手前等待输入通道就绪；安装器从版本文件读取版本号。
- 同步已有的哲理、诗句、故事及点击反馈台词。

**行为变化：单击触发说话时，也可能执行 Codex 原生的唤起主窗口动作。** 本版用于对比原生拖拽表现，实际连续拖动和跨屏手感尚待人工确认，不能仅凭自动检查宣称卡顿全部解决。

验证：Windows PowerShell 5.1 编译及脚本语法检查通过；11 个离屏场景验证全部输入放行、无辅助鼠标消息投递、手势期间无样式变化，以及点击/拖动分类和失效窗口清理。该测试不安装鼠标钩子、不注入实际输入，不验证真实 Chromium 画面流畅度。

在仓库目录运行回归检查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Test-NativeInputObserver.ps1
```
