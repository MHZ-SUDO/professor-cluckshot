# 更新记录

## 1.2.31 — 2026-09-24

修复长时间运行鼠标监听助手时的内存增长。没有消息循环的后台界面查询线程现在明确使用 MTA；正常启动和说话浮层自动恢复两条路径均带上 -MTA，避免重新进入原来的 STA 配置。WPF 说话浮层继续使用带消息循环的 STA。

输入助手和说话浮层复用当前宠物窗口的引用，只批量缓存所需属性，并让返回的元素仅携带缓存数据；窗口改变或查询失败时重新获取引用。缓存作用域通过 finally 释放。点击接收层仅在人物尺寸、按钮排除范围或所属窗口变化时重建区域，相同形状及纯位置移动复用已有区域。

保留原有点击接收层、原生拖拽交接、按钮范围、扫描周期和说话机制。没有通过定时杀进程或扩大系统虚拟内存掩盖增长。

验证：原版界面读取在第 1,000 至 3,000 次调用间增加约 14 MB；缓存读取连续 6,000 次稳定在约 79 MB。人物和按钮范围与原版一致；原生输入 11 个场景共 91 项检查及说话逻辑 48 项检查通过。区域测试验证 10,000 次相同形状仅创建一次窗口区域，并覆盖按钮移动、窗口尺寸变化、位置移动及按钮穿透。此为有限时长验证，不代表已经完成全天连续运行验收。

最终实机验证：连续观察 302 秒，两个助手均未重启；输入助手预热后提交内存从 89.969 MB 到 90.039 MB，仅变化 0.070 MB。用户确认鼠标顺滑，点击说话和拖动均正常。开机启动和浮层自动恢复均已核对使用 MTA 输入助手；自动恢复参数另经隔离测试验证。

## 1.2.30 — 2026-09-23

单击鸡哥时 Codex 主窗口会同时弹出。人物区域现在使用本地无激活点击接收层：真实按下和松开只触发鸡哥说话；拖拽越过按显示器缩放换算的原生阈值时，向 Codex 原生渲染窗口交付一次按下和首次移动。独立按钮区域保持原有输入。

后台 Codex 窗口不能保证持续收到跨出可见区域后的物理移动。拖拽交接后，接收层暂时隐藏，助手只保留最新的真实鼠标坐标，由独立窗口线程按约 16 毫秒间隔向宠物渲染窗口转发移动；松开时补齐最后一次移动和松开，再恢复接收层。接收层按照 UI Automation 人物范围定位，扣除控制按钮，使用每显示器 DPI 感知；发现截图选区等覆盖窗口时避让。异常交接会发送一次位移后的移动和松开，避免生成原生单击。隔离桌面中移动 Chromium 窗口的连续拖拽测试通过；本机使用者确认单击说话不唤起 Codex，拖拽途中连续跟随、松开不跳动。跨屏切换和第三方截图软件配合尚未单独验收。

## 1.2.26 — 2026-09-20

修复助手停止后宠物仍可见、但点击不再说话的情况，并缩短点按反馈等待。

- 通过 WMI 在后台独立启动输入和说话助手，避免继承启动端的 Windows 进程作业。
- 收到点击事件后立即触发说话，取消双击等待及随后 3 秒的点击忽略逻辑。
- 保留原生鼠标输入和 Codex 点击行为。

验证：在隔离测试中关闭启动进程的作业后，助手测试进程仍继续运行；48 项点击处理、事件去重和语法检查通过。本机两个助手独立运行，副屏气泡可见，Codex 进程及所有宠物图集和清单保持不变。函数测试不代表画面显示延迟。

2026-09-22 使用反馈：使用者确认当前版本的点击、说话和拖拽均正常。此反馈对应使用者当前环境；跨屏切换与第三方截图软件配合尚未单独验收。

在仓库目录运行点击回归检查（只提取函数测试，不操作实际鼠标或窗口）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Test-ImmediatePetSpeech.ps1
```

## 1.2.25 — 2026-09-20

Windows 宠物在拖动过程中会改变窗口位置；辅助程序提前换算坐标再异步转发时，坐标可能在消费时发生偏移。本版改用完整原生鼠标输入，辅助程序只观察手势并触发说话。

- 删除辅助程序对鼠标按下、移动和松开消息的拦截与补发。
- 拖动期间暂停 UI Automation 扫描、气泡布局测量与窗口样式维护；样式更新统一在输入线程处理。
- 保留分层窗口渲染属性，移除竖屏副屏窗口高度限制，并避让截图选区等覆盖窗口。
- 快速连续拖动时使用实际窗口命中补充忙碌状态；识别目标暂时丢失后在空闲时恢复鼠标穿透。
- 气泡跟随宠物可见性，保留长文本自动测量；只有事件文件变化时才重新解析。
- 启动说话助手前等待输入通道就绪；安装器从版本文件读取版本号。
- 同步已有的哲理、诗句、故事及点击反馈台词。

**行为变化：单击触发说话时，也可能执行 Codex 原生的唤起主窗口动作。** 本版用于对比原生拖拽表现，实际连续拖动和跨屏手感尚待人工确认，不能仅凭自动检查宣称卡顿全部解决。

验证：Windows PowerShell 5.1 编译及脚本语法检查通过；11 个离屏场景验证全部输入放行、无辅助鼠标消息投递、手势期间无样式变化，以及点击/拖动分类和失效窗口清理。该测试不安装鼠标钩子、不注入实际输入，不验证真实 Chromium 画面流畅度。

在仓库目录运行回归检查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Test-NativeInputObserver.ps1
```
